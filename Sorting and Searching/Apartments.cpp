#include <bits/stdc++.h>
using namespace std;
    
#define ll long long
    
int main(){
    int n,m;
    cin >> n >> m;
    ll k,a[n],b[m];
    cin >> k;
    for (int i = 0; i < n; i++) cin >> a[i];
    for (int i = 0; i < m; i++) cin >> b[i];
    
}