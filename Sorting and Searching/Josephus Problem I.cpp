#include <bits/stdc++.h>
using namespace std;
    
#define ll long long
    
int main(){
    int n; cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) v[i] = i+1;
    bool end = false;
    while(v.size()){
        int i;
        if(end) i = 1;
        else i = 0;
        for (; i < v.size(); i+=2)
        {
            
        }
        
    }
}